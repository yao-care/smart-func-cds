---
import OfflineBanner from '../components/common/OfflineBanner.svelte';
import { BASE_PATH } from '../../scripts/base.mjs';
import { SITE } from '../lib/seo/site';
import JsonLd from '../components/seo/JsonLd.astro';
import { organizationSchema } from '../lib/seo/schema';

interface Props {
  title: string;
  description: string;
  canonical?: string;
  noindex?: boolean;
}

const { title, description, canonical, noindex = false } = Astro.props;
const canonicalURL = canonical
  ? new URL(canonical, Astro.site)
  : new URL(Astro.url.pathname, Astro.site);
const fullTitle = `${title} | ${SITE.shortName}`;
const base = BASE_PATH;
const ogImage = new URL(base + SITE.ogImagePath, Astro.site).href;

import '../styles/global.css';
---

<!DOCTYPE html>
<html lang="zh-TW">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    {/* CSP 由 Astro security.csp 於建置期自動產生（含每頁 script/style 雜湊），見 astro.config.mjs */}
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    {noindex && <meta name="robots" content="noindex, nofollow" />}
    <link rel="canonical" href={canonicalURL.href} />
    <link rel="manifest" href={base + '/manifest.json'} />
    <link rel="icon" type="image/png" sizes="32x32" href={base + '/icons/favicon-32.png'} />
    <link rel="icon" type="image/png" sizes="16x16" href={base + '/icons/favicon-16.png'} />
    <link rel="apple-touch-icon" sizes="180x180" href={base + '/icons/apple-touch-icon.png'} />
    <meta name="theme-color" content="#3d6b54" />
    <meta name="author" content={SITE.organization.legalName} />
    <meta name="application-name" content={SITE.shortName} />
    <link rel="alternate" type="application/rss+xml" title="衛教內容" href={new URL(base + '/rss.xml', Astro.site).href} />

    <!-- Open Graph -->
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="zh_TW" />
    <meta property="og:url" content={canonicalURL.href} />
    <meta property="og:site_name" content={SITE.name} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content={SITE.name} />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={ogImage} />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={description} />

    {/* 公司節點必須出現在「每一頁」，頁面自己的節點才有 @id 可以參照；
        只放首頁會讓所有內頁的 publisher 參照懸空。此處是全站唯一的定義處，
        頁面層請用 orgRef 參照，不要重複定義。 */}
    <JsonLd data={organizationSchema()} />

    <slot name="head" />
  </head>
  <body>
    <a href="#main-content" class="skip-to-content">跳至主要內容</a>
    <OfflineBanner client:idle />
    <slot />
    <script src="../lib/sw/register-entry.ts"></script>
  </body>
</html>
